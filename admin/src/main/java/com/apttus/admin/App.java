package com.apttus.admin;

import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.firefox.FirefoxDriver;

import com.apttus.admin.Helper;

/**
 * Hello world!
 *
 */
public class App 
{
    public static void main( String[] args ) throws InterruptedException, Exception
    {
    	/*WebDriver driver=new ChromeDriver();
    	Helper objcpqadmin=new Helper(driver);
    	objcpqadmin.SendkeysById("cred_userid_inputtext", "globaladmin@aicaqlstg.apttuscloud.io")
		  .SendkeysById("cred_password_inputtext", "f8uthuC*e6tu")
		  .sleepWait(2).clickById("cred_sign_in_button");
    	
    	WebElement tab=driver.findElement(By.tagName("table"));
    	List<WebElement> th=tab.findElements(By.tagName("th"));
    	for (WebElement t:th) {
    		System.out.println(t.getText().toString());
    	}*/
    	
    }
}

